<section class="opl-simple-page page-width">
  <h1>{{ page.title }}</h1>
  {% if page.content != blank %}
    <div class="rte">{{ page.content }}</div>
  {% else %}
    <p>Olympus Performance Labs builds premium performance supplements designed for athletes who value discipline, performance, and transparency.</p>
    <p>hello</p>
  {% endif %}
</section>

{% schema %}
{
  "name": "OPL Simple Page",
  "settings": [],
  "presets": [{ "name": "OPL Simple Page" }]
}
{% endschema %}